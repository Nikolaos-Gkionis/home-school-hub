<%# locals: title: nil %>
<div class="hub-card">
  <% if local_assigns[:title].present? %>
    <p class="mb-3 font-display text-lg font-semibold text-[var(--color-text)]"><%= title %></p>
  <% end %>
  <%= yield %>
</div>

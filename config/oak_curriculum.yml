# Oak Curriculum API sync — only the subjects we teach in the hub.
# Request an API key: https://open-api.thenational.academy/docs
# Run: OAK_API_TOKEN=... bin/rails curriculum:oak_sync
#
# Subject slugs must match GET /subjects (Oak). If a slug 404s, adjust it here.
# Oak's maths slug still uses the display name "Mathematics".

year_groups:
  - year_7
  - year_8

subjects:
  - slug: maths
    display_name: "Mathematics"
  - slug: english
    display_name: "English"
  - slug: science
    display_name: "Science"
  - slug: history
    display_name: "History"
  - slug: music
    display_name: "Music"
